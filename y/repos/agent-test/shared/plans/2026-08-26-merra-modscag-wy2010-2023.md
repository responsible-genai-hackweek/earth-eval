# Water years 2010-2023 STC-MODSCAG comparison

Status: implemented and validated with an authenticated month-level smoke test

Planning date: 2026-08-26; checkpoint/resume revision: 2026-08-26

## 1. Fixed scientific scope

- Water years 2010 through 2023, inclusive
- Dates: 2009-10-01 through 2023-09-30
- 14 water years, 168 calendar months, and 5,113 calendar days
- Domain: 72 MERRA-2 cells centered within 109-104°W and 37-41°N
- Reference: STC_MODSCGDRF_HIST v1 snow_fraction / 100
- Model: MERRA-2 M2T1NXLND v5.12.4 FRSNO, index 15 (15:00-16:00 UTC mean, timestamped 15:30 UTC)
- MERRA filename stream: 300 through 2010; 400 beginning in 2011 except for reprocessed stream 401 in September 2020 and June-September 2021
- Error sign: MERRA-2 minus STC-MODSCAG
- Target-cell support: at least 80% of complete-cell 500 m pixel-center support

This avoids splicing the historical STC-MODSCAG output with the different post-2023 NRT product. The historical output changes its MOD09GA input from collection 6 to 6.1 on 2021-12-28 but remains one published output product and version.

## 2. Resumable 16-worker execution

### Calendar-month task unit

Use 168 independent calendar-month tasks in a spawn-based ProcessPoolExecutor(max_workers=16). A process creates one authenticated Earthdata session and one static MODIS-to-MERRA mapping, then can accept multiple month tasks. Within a month it:

1. Processes dates sequentially.
2. Requests the 72-cell MERRA subset for each date.
3. Downloads the three available MODSCAG tiles into a private temporary directory.
4. Reads only the relevant tile crops, reduces them, and deletes each granule immediately.
5. Atomically writes one validated monthly comparison-statistics checkpoint.

The month is the minimum recomputation unit. No daily tables, collocated arrays, source granules, regridding caches, or partial-month files are accepted as checkpoints.

### Planned shutdown and hard interruption

`--max-runtime-minutes 30` stops scheduling new months at the deadline and lets the at-most-16 in-flight months finish. The command can therefore exceed the requested duration by one month-task runtime. After "paused cleanly" is printed, the computer can be shut down. Rerunning the same command validates existing checkpoints and schedules only missing or invalid months.

Each CSV is written to a uniquely named temporary file, flushed and fsynced, then promoted with an atomic filesystem replacement. A power loss during a write cannot turn a partial file into a valid month. Workers ignore SIGINT so the parent can stop submission while in-flight workers finish their current atomic checkpoints.

### Apple silicon concurrency

macOS assigns runnable processes to performance and efficiency cores; Python has no supported portable P-core affinity interface. Sixteen normal-priority processes allow macOS to use the available performance cores. The CLI sets VECLIB_MAXIMUM_THREADS=1, OMP_NUM_THREADS=1, and OPENBLAS_NUM_THREADS=1 before NumPy is imported to prevent nested numerical thread teams.

Only 16 tasks are submitted at once. This bounds process memory and enables a runtime deadline to stop future work promptly. Each worker performs one sequential MERRA request and one MODSCAG tile download at a time; there is no nested network pool. All processes share an eight-slot semaphore around FTP downloads because the archive rejects ten simultaneous connections from one IP. A 421 response uses staggered 5/10/20-second backoff, and a failed month is placed at the back of the queue before its second attempt.

## 3. Monthly checkpoint contract

The range-specific checkpoint directory contains YYYY-MM.csv. Each of the 168 valid files has 73 statistic rows: 72 MERRA cells and one domain row. Every row records:

- sum_w
- sum_w_error
- sum_w_abs_error
- sum_w_r
- valid_pixels
- expected_pixels
- observed_pixels
- n_cell_days
- n_days
- n_calendar_days
- bias_pp
- mae_pp
- support_fraction
- direct_observation_fraction

**Departure from the original 13-column list (implementation note):** `sum_w_r` (sum of weight times MODSCAG reference fraction R) was added as a 14th sufficient-statistic column. NMB and NMAE, as specified in statistics-and-figures.md for the wet/dry composite and significance figures, are normalized by the paired MODSCAG signal — that denominator is `sum_w_r / sum_w` and cannot be reconstructed post hoc from `bias_pp`/`mae_pp` alone. The same ratio is also exactly the "composite MODIS fSCA" statistic used for the 0.05/0.10 masking thresholds, so this one column serves both needs. See the matching note in `src/fsca_eval/config.py` (`CHECKPOINT_COLUMNS`).

The sufficient sums make later monthly, seasonal, and climatological merges exact; pre-averaged bias and MAE are never averaged together. Checkpoint metadata includes the cell identity, product versions, error sign, timing, aggregation, domain, schema version, and a SHA-256 fingerprint of the scientific configuration.

A checkpoint is reusable only if all of these pass:

- Exact column schema and 73-row stable slot order
- Matching configuration fingerprint and comparison metadata
- Matching cell coordinates, global indices, and stable cell_id
- Correct number of calendar days for that month
- Nonnegative and internally consistent support/count fields
- abs(bias) <= MAE and agreement between stored metrics and sufficient sums
- Reconstruction of domain additive statistics from the 72 cell rows

An invalid file is reported and recomputed atomically. Complete checkpoints can rebuild the final aggregate files without Earthdata access.

## 4. Pixel-level and domain aggregation

For a cell-day, MERRA fSCA is compared with the mean of its accepted equal-area MODSCAG pixels. Daily errors are weighted by the accepted MODSCAG pixel count. The domain metric pools those weighted cell-days. Each cell retains its own paired-day and missing-reference counts; the domain also retains the number of calendar days with at least one paired cell.

Final groups are:

- 14 water years x 12 months
- 14 water years x 4 meteorological seasons
- pooled WY2010-WY2023 climatology x 12 water-year months
- pooled climatology x 4 seasons

Expected final row counts:

- overall: 240
- per-cell: 17,280

Every cell receives a row for every group. A group with zero pairs uses a null metric and explicit zero counts rather than omitting the row.

## 5. Satellite image examples (before and after regrid)

To illustrate the aggregation and regridding process, render one high-snow day and one low-snow/transition day showing all four views across the full domain:

### Example days
- **High-snow example:** 2011-01-15 (peak winter, ~70% domain snow cover)
- **Low-snow example:** 2015-06-01 (transition season, ~10% domain snow cover)

These four panels faithfully illustrate the actual comparison pipeline (scientific-contract.md). No bilinear resampling is introduced: MERRA-2 is never resampled to MODIS resolution, at any point, for metrics or for illustration. Aggregation flows only from fine MODIS pixels up to the native MERRA cell, per the established equal-area pixel-center mean.

### Outputs per day
1. **MODIS raw:** 500 m native MODSCAG snow_fraction (fSCA), ~500×200 pixels per domain, before any aggregation
2. **MODIS aggregated to MERRA (`R`):** equal-area pixel-center mean of valid 500 m pixels within each 0.625°×0.5° cell — this is the exact reference value used in the error computation
3. **MERRA2 raw (`M`):** 0.625°×0.5° native FRSNO at index 15, ~60×40 pixels, used as-is with no resampling
4. **Paired `M` vs `R` at the shared MERRA grid:** side-by-side or difference (`M - R`) panel at the 72-cell domain, using the diverging red/blue scale centered at zero from statistics-and-figures.md

Panel 2 and panel 3 are literally the two inputs to `error = M - R`; panel 4 visualizes that computation directly. Panels use the coarse USGS 3DEP hillshade under the metric layer and major elevation contours (2,000 m and 3,000 m), consistent with the repository's other spatial figures. Label panel 2's support fraction and pixel counts for that day so the 80% threshold is visibly satisfied.

### File format and storage
Each day produces:
- One NetCDF with the four 2D arrays at their native grids (MODIS 500 m for panel 1, MERRA 72-cell grid for panels 2–4), lat/lon coords, spatial reference, fSCA values 0–100%
- One 4-panel PNG figure with titles, shared colorbar for panels 2–3 (sequential, nonnegative fSCA), diverging colorbar for panel 4 (signed `M - R`), hillshade and elevation contours per statistics-and-figures.md
- Metadata in both files: product versions, timestamp (UTC), domain bounds, aggregation method, pixel counts, support fraction

Estimated size: ~1 MB per day (NetCDF) + 500 KB per day (PNG, larger due to hillshade/contours) × 2 days = **~3 MB total**.

### Integration
- Generate after all 168 checkpoints validate and before final CSV/plot writes
- Use the same cell grid and MODIS-to-MERRA mapping as the checkpoint pipeline
- Log pixel counts and support fractions for each aggregation to confirm 80% threshold is met
- These images are illustrative only; they do not feed back into the checkpoint statistics or aggregate metrics

## 6. Persistent artifacts

The resumability request supersedes the earlier three-files-only rule. The pipeline persists only comparison statistics, visual outputs, and example satellite imagery:

- results/water_year_2010_2023_monthly_checkpoints/ - 168 month CSVs
- results/water_year_2010_2023_overall_stats.csv
- results/water_year_2010_2023_pixel_stats.csv
- results/water_year_2010_2023_bias_mae.png
- results/water_year_2010_2023_example_high_snow_20110115.nc
- results/water_year_2010_2023_example_high_snow_20110115.png
- results/water_year_2010_2023_example_low_snow_20150601.nc
- results/water_year_2010_2023_example_low_snow_20150601.png

The aggregate CSVs, bias/MAE plot, and example images are written only when all 168 checkpoints validate. Total persistent size: ~5–6 MB.

The example NetCDF/PNG files are illustrative outputs of the already-validated pipeline; they are not inputs to, and do not alter, the checkpoint statistics, aggregate CSVs, or bias/MAE plot.

## 7. Missing-reference policy

- All-fill MODSCAG days are counted as missing and excluded; no new temporal interpolation or carry-forward is applied.
- Cell-days below 80% support are null for that cell only.
- Missing-reference counts remain separate for each cell and the domain.
- The known 2022-10-10 through 2022-10-27 all-fill interval must reproduce the completed WY2023 count of 18 missing domain-reference days.

## 8. Validation record and remaining full-run gates

Completed during implementation:

- 14 offline tests cover dates, grids, stream boundary, metric weighting, domain reconstruction, checkpoint round-trip and config mismatch rejection, final row counts, MODSCAG fill masking, and multi-year plot generation.
- Authenticated preflight decoded MERRA subsets on 2009-10-01 (stream 300) and 2023-09-30 (stream 400), confirming 72-cell coordinates and index 15.
- An authenticated end-to-end run completed October 2009, atomically wrote its 73-row checkpoint, reloaded it, and left no worker temporary directory.

Gates applied when the record finishes:

- Verify all 5,113 dates across 168 valid checkpoints.
- Decode MODSCAG before and after the 2021 MOD09GA collection transition.
- Verify 240 overall and 17,280 per-cell final rows with no duplicate keys.
- Reconstruct each overall group from its 72 cell accumulators.
- Reproduce completed WY2023 domain metrics within floating-point tolerance.
- Confirm example days are found in checkpoint range (2011-01-15 and 2015-06-01).
- Verify both example-day NetCDF files contain valid coordinates, four 2D arrays at their native grids, and metadata.
- Confirm panel 2 (MODIS aggregated to MERRA) and panel 3 (MERRA raw) match the `R` and `M` values already stored in that day's monthly checkpoint.
- Confirm no bilinear or other resampling of MERRA-2 appears anywhere in the example pipeline or its outputs.
- Visually inspect example-day PNG figures for correct domain extent, sequential vs. diverging colorbar scales, hillshade/contour legibility, and pixel-level detail in panel 1.
- Confirm only monthly checkpoints, three aggregate files, and four example-day files persist (~5–6 MB total).
