% 07. MERRA-2 against MODSCAG, mean fSCA bias, split DJF against MAM.
% figures/merra2_modscag_seasonal_bias.png, alone on the frame.
%
% THIS REPLACED A DIFFERENT FIGURE and the whole frame changed with it. What
% stood here was wy2023_wet_dry_composite_rot.png: MERRA-2 minus SPIReS,
% NORMALIZED bias, seven months across and wet water years against dry down.
% The new figure shares none of that — different reference product, different
% units, different split — so the frametitle, the caption and the sizing are all
% new rather than edited. The old file is still in figures/ and is now unused.
%
% THE REFERENCE PRODUCT CHANGED, SPIReS to MODSCAG. That is not a relabelling:
% research/FSCA_PRODUCT_NOTES.md is explicit that the two are different
% spectral-mixture retrievals and must never be swapped in a caption. The new
% figure's own colorbar says MODSCAG, so the caption says MODSCAG — and the
% deck is now consistent, because 08 and 09 are MODSCAG too. The SPIReS
% comparison no longer appears anywhere in the deck.
%
% PERIOD IS INFERRED, NOT READ OFF THE FIGURE. The panels carry no years. It is
% given as WY2010-2023 because that is the range of the only multiyear MODSCAG
% comparison in the repo (plan/MULTIYEAR_MODSCAG_PLAN.md is titled "Water years
% 2010-2023 STC-MODSCAG comparison") and because 72 is this domain's cell count,
% matching the panels' 53/72 and 58/72. CONFIRM BEFORE THE TALK: unlike every
% other number in the deck this one is not traceable to results/FINDINGS.md.
%
% SIZE. The figure is 1279x947, so 1.351:1 — nearly square by this deck's
% standards and HEIGHT-bound on a 16:9 page. Widening its box does nothing; only
% the height figure moves it. At 6.34cm it sets 8.56cm wide against a 14.9cm
% text block, which leaves about 3cm of white either side. That is the honest
% cost of a near-square figure on a wide page, and the alternative is a text
% column beside it rather than a bigger figure.
%
% The caption states only what the panels do not. DJF, MAM and the two
% significance counts are all printed on the figure itself, so repeating them
% would be spending caption on what the canvas already says.
\begin{frame}[t]
\frametitle{fSCA Bias, Winter against Spring}
\vspace{0.30em}
{\centering\includegraphics[height=6.34cm]{merra2_modscag_seasonal_bias.png}\par}
\vspace{0.45em}
\figcaption{MERRA-2 minus MODSCAG, mean fSCA bias, WY2010--2023.
The model runs low against the satellite in both seasons.}
\end{frame}
