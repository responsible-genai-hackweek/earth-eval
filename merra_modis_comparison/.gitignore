.DS_Store
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.mypy_cache/

# Rendered figures are build artefacts, not data. They regenerate from the
# checkpoints with no network access, so tracking them only churns the history.
results/*.png
results/*.pdf
